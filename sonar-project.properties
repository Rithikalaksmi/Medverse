sonar.projectKey=epiguard-fullstack
sonar.organization=your-org-name

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=epiguard-fullstack
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=epiguard/src,epiguard-backend/app

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Exclude node_modules from analysis
sonar.exclusions=**/node_modules/**,**/venv/**,**/dist/**
